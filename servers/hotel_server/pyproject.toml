[project]
name = "hotel-server"
version = "0.1.0"
description = "MCP server for hotel and vacation rental search using SerpAPI"
readme = "README.md"
requires-python = ">=3.12.1"
dependencies = [
    "fastmcp>=2.5.1",
    "mcp>=1.9.1",
    "python-dotenv>=1.1.0",
    "requests>=2.32.3",
]
