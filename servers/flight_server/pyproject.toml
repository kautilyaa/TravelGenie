[project]
name = "flight-server"
version = "0.1.0"
description = "MCP server for flight search using SerpAPI"
readme = "README.md"
requires-python = ">=3.12.1"
dependencies = [
    "fastmcp>=2.5.1",
    "mcp>=1.9.1",
    "requests>=2.32.3",
]
